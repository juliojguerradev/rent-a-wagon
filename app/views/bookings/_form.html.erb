<%= simple_form_for @booking do |f| %>
  <%= f.input :start_date, as: :date %>
  <%= f.input :end_date, as: :date  %>
  <%= f.association :vehicle, prompt: "Choose a vehicle", value_method: :id%>
  <%= f.button :submit %>
<% end %>
